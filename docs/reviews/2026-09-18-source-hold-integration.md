# Current-base source-hold integration — 18 September 2026

This completes the pending #125 implementation on accepted main
`1ca1112dee406327793f9b3e23c4291b8c8d13c7`, preserving its original source
note/fixtures and the explicit value-hold identity safeguard from #128. The merge
retains both histories; the older proposal's generated data is not authoritative.
The three later automated data publications remain intact (1,370 current records).

## Problem and acceptance

A populated field can still have a documented conflict. Re-extraction must not
remove that unresolved work from the validation/repair/P4 gate. The same shared
hold matcher now governs both public withholding and actionable manual tasks.
Eleven active held fields add eleven review items to the existing 1,596 reviews.
These are retained obligations, not eleven newly discovered errors or resolved
repairs. Every original review, 441 proposals, missing-field rule, priority and
automatic collector gap is preserved. No canonical replacement is guessed.

Snehaa's lead-manager value combines two role columns. Sacheerome's lead-manager
value does the same, and its registrar value is only a former-name qualifier.
Exact issue/document/value holds suppress those three strings while preserving
raw values, field proofs, original timestamps and correction histories. The
original [column review](2026-09-18-intermediary-column-review.md) remains unchanged.
Identity restrictions include the offer date in the supporting proof. Emmvee's
completed correction is not put back under its historical value hold. Teamtech's
document conflict remains held. P5/performance expansion remains blocked.

## Safe publication boundary

A review-policy change selects `review` in the existing serialized publisher.
This rebuilds only validation, queue, phase and public artifacts from current main.
It skips source/residual collection, correction application and proposal merging.
Protected canonical and pending-proposal bytes must not change. Unknown or mixed
collector changes still use the guarded source path. Permissions, schedules and
source-manifest rules are unchanged; there is no additional writer.

The small phase report accompanies the code for the existing correctness tests.
Large queue/validation/profile outputs are rebuilt in CI and by that bounded
publisher, not copied from #125's old snapshot. Code merge alone is not final
release acceptance: require the publisher commit, review/hold preservation and
actual served profile/queue/phase verification after it completes.

## Checks and limitations before release

All 1,125 local regressions and eight Node quality tests pass through uv. The
complete original hold fixtures and #128 identity tests are included. All 1,596
prior review occurrences survive; eleven manual tasks are added, zero unmapped.
The candidate gate is 1,607 total reviews / 1,603 P4 blockers, 387 actionable plus
55 higher-priority records. Regeneration changes only two company profiles, with
1,368 unchanged; canonical data and all 441 proposals are byte-identical.

Local frozen sync lacked locked dependencies; the local Python 3.13.5 pass is not
a frozen pass. Complete-repository frozen CI and browser acceptance are required.
The browser check covers actual profiles and quick views at 1440/390/320 pixels,
malformed-name absence, source links, overflow and canonical-fetch avoidance.
The existing smoke, freshness and reviewed-composition journeys remain required.

The narrow read-only source check retrieves only the two official Final Prospectuses,
verifies full file hashes/lengths/page counts and exact retained field bindings,
and retains only the two selected pages for visual review. This is not a full
financial-disclosure audit. Source download/hash success alone does not accept
replacement entities. Inspect the page artifacts before acceptance. No full PDFs
are added to public payloads, and public access does not confer redistribution rights.
Source rights, audience, pricing and privacy remain unresolved. No new spending,
contracts, outreach, tracking, billing or material access changes are introduced.

Final CI, source-page and deployed receipts will be recorded in PROJECT_STATUS
and docs/releases after acceptance. Next separate work is diagnosing overdue
subscription collection using current source/run evidence, not another writer.
