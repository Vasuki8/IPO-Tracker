The final bounded source review executed at **2026-09-18T00:37:05.669681+00:00** against actual remote commit `409c51c939bd839940594278d324016766cb84c6`, tree `22d2f1cdf99483755a0cd0032ece03033f065784`, with current main base `dd3a9b42057422057c98a16c9b1281cf318a54d8`. A subsequent `git ls-remote` confirmed the exact commit on `p4-source-review-code-freeze-2026-09-18`. The [compact execution receipt](2026-09-18-source-review-409c51c9.json) and [unaltered full producer receipt](source-acceptance-receipt.json) record the actual code, registry, 115 code/policy files, PDF and output hashes. The historical `d8eed055…` and `ba24d54a…` executions remain separate lineages. The original `5516b3ad…` proposal remains unavailable; this reconstruction neither recovers its identity nor invalidates it.

**All 80 technical checks passed**, and both candidate payloads have zero strict validation errors. The production parser consumed all **511 Emmvee pages** and **372 Teamtech pages** from exact recovered PDF bytes, with no new PDF downloads or producer network calls. The seven-record diagnostic preserves the other **1,359 records** and all payload metadata. It appends four Emmvee correction events and one quarantine event for each of six held issuers; its 1,624 review items remain present. It remains **UNACCEPTED for publication**.

Teamtech remains null under a **document-scoped quarantine after the final policy pass**, pending revalidation with no live objects field proof. Its [four-row parser evidence](teamtech-UNACCEPTED-parser-evidence.json), totaling ₹45.4859 crore, remains explicitly unaccepted. Physical/printed page 89 labels the same net-proceeds number as crores, conflicting with the lakh tables on pages 22–23 and 87. Structural validity does not reconcile those units. Existing holds and historical evidence for Unimech, Blackbuck, M&B Engineering, Shri Ahimsa and GenXAI remain intact. Only the first three of those five source PDFs were recovered and hash-checked here; none received a new complete source review in this reconstruction.

The separate Emmvee-only source check supports **₹2,143.862 crore fresh issue, ₹756.138 crore OFS and ₹2,900 crore total**. Its [exact diff](SOURCE_CHECK_PASSED-emmvee-only-exact-diff.json) changes four composition fields and their [four matching proofs](emmvee-source-checked-field-proofs.json), appending four correction events. The existing allocations of **₹1,621.294 crore and ₹438.711 crore**, their entire proof, unrelated field proofs, whole-document metadata, prior correction history and all other **1,365 records** remain unchanged. Its 1,618 review items remain present. The source’s Basis of Allotment qualification is retained; no later allotment outcome is claimed. This candidate has not been accepted for publication or published.

The [independent replay audit](replay-verification.json) passed **40 additional checks**: all 13 producer outputs were compared, with 11 byte-identical and only the two validation generation timestamps differing. Both exact diffs independently reproduce their payloads. The frozen checkout and canonical/code/policy bytes remained unchanged. The portable audit omits only transient execution-directory fields and records the original audit hash. The code/base freeze requirement is satisfied by this actual execution; the full receipt’s generic refreeze wording does not imply that the recorded freeze is missing. A separately accepted targeted publication transport remains outstanding. The broad automatic source preview has a different scope and cannot substitute for that acceptance. P4 remains blocked; this work does not enable P5 or performance expansion.

The full seven-record exact diff is stored as [deterministic gzip](UNACCEPTED-seven-record-exact-diff.json.gz), with gzip timestamp zero. Decompressing it recovers the original producer filename and SHA-256 `4ed7f3e48b349d9f418be294f606d5b058ff15dab39b74411ab15833712beedb`; the raw receipt’s output names and hashes are unchanged:

```sh
gzip -dk UNACCEPTED-seven-record-exact-diff.json.gz
sha256sum UNACCEPTED-seven-record-exact-diff.json
```

The [unaccepted candidate archive](UNACCEPTED-candidate-records.json.gz) stores the seven changed record snapshots, exact base/code/receipt pins and expected output hashes. The [archive recovery check](candidate-archive-recovery.json) recovered both complete candidate payloads byte for byte from the immutable base and archive, without PDFs. For each output, load that base, replace only the listed `changedIds` from `archive.records` while retaining record order and metadata, serialize JSON with `indent=2`, `ensure_ascii=False` and a trailing newline, then verify the recorded output hash. This preserves already generated diagnostic bytes; renewed source review still requires the exact PDFs. The archive remains explicitly unaccepted and is not publication transport.

To reproduce the diagnostic from sources, use a clean checkout of the exact commit above. Set `PDF_DIR` to a directory containing `emmvee.pdf`, `teamtech.pdf` and the three recovered retained-hold PDFs with hashes matching the receipt; set `OUT` to a new directory outside that checkout. The exact timestamp reproduces payload and evidence bytes; validation reports still record their own generation time.

```sh
uv run --frozen python scripts/reconstruct_source_review.py \
  --code "$PWD" \
  --expected-code-commit 409c51c939bd839940594278d324016766cb84c6 \
  --expected-base-commit dd3a9b42057422057c98a16c9b1281cf318a54d8 \
  --expected-registry-sha256 14bb49fa3069d8adf31231cf8e0c09eb9d431cf4e0c21133f3d31bf41fdc7636 \
  --emmvee-pdf "$PDF_DIR/emmvee.pdf" \
  --teamtech-pdf "$PDF_DIR/teamtech.pdf" \
  --retained-pdf-dir "$PDF_DIR" \
  --out "$OUT" \
  --reviewed-at 2026-09-18T00:37:05.669681+00:00
```

Any later source-selector, policy or canonical-base change requires a new execution against its actual immutable commit. No canonical files, code, workflows, remote resources or deployments were modified by this source check.
